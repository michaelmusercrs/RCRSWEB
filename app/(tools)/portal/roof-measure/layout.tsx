import { portalMetadata } from '../metadata';

export const metadata = portalMetadata('Roof Measure');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
