import { ccMetadata } from '../metadata';

export const metadata = ccMetadata('Roof Measure Tool');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
