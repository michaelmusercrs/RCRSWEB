import { ccMetadata } from '../../metadata';

export const metadata = ccMetadata('Job Profitability Report');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
