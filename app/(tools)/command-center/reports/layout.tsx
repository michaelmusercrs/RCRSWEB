import { ccMetadata } from '../metadata';

export const metadata = ccMetadata('Reports & Analytics');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
