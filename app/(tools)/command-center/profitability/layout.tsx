import { ccMetadata } from '../metadata';

export const metadata = ccMetadata('Job Profitability');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
