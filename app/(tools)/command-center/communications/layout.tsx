import { ccMetadata } from '../metadata';

export const metadata = ccMetadata('Communications Hub');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
