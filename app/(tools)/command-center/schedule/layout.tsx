import { ccMetadata } from '../metadata';

export const metadata = ccMetadata('Schedule Management');

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
